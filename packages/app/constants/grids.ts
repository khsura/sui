export enum GridAlignProperty {
  baseline = 'baseline',
  center = 'center',
  end = 'end',
  start = 'start',
  stretch = 'stretch',
}

export enum GridJustifyProperty {
  center = 'center',
  end = 'end',
  spaceAround = 'spaceAround',
  spaceBetween = 'spaceBetween',
  start = 'start',
}
