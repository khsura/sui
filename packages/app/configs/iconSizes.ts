import { SizeProperty } from '~/constants/size'

export const iconSizes = {
  [SizeProperty.mini]: 14,
  [SizeProperty.small]: 18,
  [SizeProperty.default]: 24,
  [SizeProperty.large]: 32,
  [SizeProperty.extra]: 40,
}
