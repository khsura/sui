import { SizeProperty } from '~/constants'
import { type SizePropertyType } from '~/types'

export const sizeProperties: SizePropertyType[] = [
  SizeProperty.mini,
  SizeProperty.small,
  SizeProperty.default,
  SizeProperty.large,
  SizeProperty.extra,
]
