import { Meta, Story, Controls, Canvas } from '@storybook/blocks';

import * as BadgeStories from './badge.stories'

<Meta of={BadgeStories} />


# Badge
The `SBadge` component superscripts or subscripts an avatar-like icon or text onto content to highlight information to a user or to just draw attention to a specific element. Content within the badge usually contains numbers or icons.

<Canvas of={BadgeStories.Badge}/>
<Controls of={BadgeStories.Badge} />


<Canvas of={BadgeStories.BadgeWithSlot}/>
<Controls of={BadgeStories.BadgeWithSlot} />
